# 如何用域名访问？

首先将域名 `A` 记录解析到服务器 IP, 这是即可通过 `域名:端口` 访问，如果不想带端口，可使用 `nginx` 或 `caddy` 等工具反向代理. 以下以宝塔面板为例:

首先点击 `网站` -> `新增站点`:

<img src="/img/2020/01/30/558b35c21519c.png" className={'sm:w-2/3'}></img>

点击反向代理:

<img src="/img/2020/01/30/2bf2585516f8d.png" className={'sm:w-2/3'}></img>

设置反向代理:

<img src="/img/2020/01/30/ac865a78a617b.png" className={'sm:w-2/3'}></img>