# 如何查看 ZFile 版本？

## 登录后台查看

进入后台「仪表盘」，在构建信息中可查看后端版本、Git 提交和构建时间。

## 通过浏览器控制台查看

在 `Chrome/Firefox` 系浏览器下打开 `ZFile` 任意页面，然后按 `F12` 键打开 `DevTools`，点 `Console` 栏，可看到版本信息。

<img src="/img/2022/08/16/rWybqY.png" className={'sm:w-1/2'}></img>